<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/Default">大松ERP</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto">
            @if (PM.Any(UPM.采购))
            {
                <li class="nav-item">
                    <a class="nav-link" href="/Order/New">新建采购订单</a>
                </li>
            }
            @if (PM.Any(UPM.采购, UPM.跟进))
            {
                <li class="nav-item">
                    <a class="nav-link" href="/Order">订单操作</a>
                </li>
            }
            @if (PM.Any(UPM.拆包))
            {
                <li class="nav-item">
                    <a class="nav-link" href="/Order/ChaiBaoList">拆包审单</a>
                </li>
            }
            @if (PM.Any(UPM.电话))
            {
                <li class="nav-item">
                    <a class="nav-link" href="/Order/Import">电话客服订单导入</a>
                </li>
            }
            @if (PM.Any(UPM.售后))
            {
                <li class="nav-item">
                    <a class="nav-link" href="/Order/ShouHouList">售后列表</a>
                </li>
            }
            @if (PM.Any(UPM.客服))
            {
                <li class="nav-item">
                    <a class="nav-link" href="/Order/KeFuList">客服列表</a>
                </li>
            }
            @if (PM.Any(UPM.管理))
            {
                <li class="nav-item">
                    <a class="nav-link" href="/User">用户管理</a>
                </li>
            }
        </ul>
        <div class="form-inline my-2 my-lg-0">
            @if (ViewData["UserID"] != null)
            {
                <a href="/SignOut">
                    注销
                </a>
            }
        </div>
    </div>
</nav>